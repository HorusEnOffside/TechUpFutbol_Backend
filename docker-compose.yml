services:

  # ================================
  # Base de datos PostgreSQL
  # ================================
  db:
    image: postgres:16-alpine
    container_name: techcup-postgres-db
    env_file:
      - .env
    restart: unless-stopped
    environment:
      POSTGRES_DB: techcup
      POSTGRES_USER: techcup_owner
      POSTGRES_PASSWORD: techcup123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U techcup_owner -d techcup"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ================================
  # Backend Spring Boot
  # ================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: techcup-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://db:5432/techcup?stringtype=unspecified
      CORS_ALLOWED_ORIGINS: "https://localhost:3000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173"
    ports:
      - "8443:8443"
    volumes:
      - ./logs:/app/logs

volumes:
  postgres_data: